const express = require('express');
const router = express.Router();
const carritoDetalleController = require('../controllers/carritoDetalle.controller');

// router.get('/', carritoDetalleController.getAll);

module.exports = router;
