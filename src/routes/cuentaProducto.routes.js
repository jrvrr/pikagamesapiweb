const express = require('express');
const router = express.Router();
const cuentaProductoController = require('../controllers/cuentaProducto.controller');

// router.get('/', cuentaProductoController.getAll);

module.exports = router;
