const express = require('express');
const router = express.Router();
const promocionProductoController = require('../controllers/promocionProducto.controller');

// router.get('/', promocionProductoController.getAll);

module.exports = router;
