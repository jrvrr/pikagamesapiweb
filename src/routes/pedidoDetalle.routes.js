const express = require('express');
const router = express.Router();
const pedidoDetalleController = require('../controllers/pedidoDetalle.controller');

// router.get('/', pedidoDetalleController.getAll);

module.exports = router;
